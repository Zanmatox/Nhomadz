{% extends 'base.html.twig' %}

{% block title %}Nhomadz - {{ product.name }}{% endblock %}

{% block content %}

<div class="row">
    <div class="col-md-5">
        <img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="imgfluid">
    </div>
    <div class="col-md-7 my-auto">
    <h3>{{ product.name }}</h3>
    <p>{{product.subtitle}}</p>
    <span class="product-page-price">{{ (product.price /100)|number_format(2, ',', '.') }}$</span>
    <hr>
    <p>
    {{ product.description }}
    </P>
    <hr>
    <a href="" class="btn btn-primary">Add to cart</a>
    </div>
</div>
{% endblock %}
 